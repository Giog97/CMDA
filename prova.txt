ciao gionni faccio una prova per GitHub :)

Ho aggiunto i branch. Il mio è MaryGio e il tuo è Gionni.

- git checkout Gionni 
è il comando per cambiare la branch.

- git branch -a
è il comando per verificare in quale branch sei.

- git checkout main
è il comando per ritornare alla branch principale. 

- git branch -d Gionni
è per eliminare la branch 

- git merge Gionni
è il comando per mergiare la branch Gionni alla main 

-git reset --soft HEAD~1
è il comando per eliminare l'ultimo commit

Per allinearsi con il main:
(partire dalla branch personale)
         
- git fetch origin               
- git merge origin/main